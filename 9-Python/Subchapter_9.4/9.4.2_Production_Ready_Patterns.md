### 9.4.2 Production-Ready Patterns: Writing Robust Automation

#### Why Production-Ready Matters

Scripts that work on your laptop may fail in production. Production-ready code handles:
- **Configuration** – Different environments (dev/staging/prod)
- **Retries** – Transient failures (network blips, rate limits)
- **Graceful shutdown** – Clean up resources on SIGTERM
- **Structured logging** – For debugging and monitoring
- **Error handling** – Fail gracefully, not crash

This note covers production patterns. Note 9.4.1 covered testing; note 9.4.3 is the subchapter review with final exam.

**Backward references:** Logging from 9.3.1; error handling from 9.3.1; environment variables from 9.2.2; subprocess from 9.2.1.

---

## Part 1: CLI Script Structure – `if __name__ == "__main__"`

### Standard Pattern

```python
#!/usr/bin/env python3
"""
My Production Script

Usage:
    python script.py --config config.yaml --verbose
"""

import argparse
import logging
import sys
from pathlib import Path

# Constants
DEFAULT_CONFIG = {
    'log_level': 'INFO',
    'retry_count': 3,
    'timeout': 30
}

def parse_args():
    """Parse command-line arguments"""
    parser = argparse.ArgumentParser(description='My Production Script')
    parser.add_argument('-c', '--config', help='Configuration file path')
    parser.add_argument('-v', '--verbose', action='store_true', help='Verbose output')
    parser.add_argument('--dry-run', action='store_true', help='Dry run (no changes)')
    return parser.parse_args()

def setup_logging(verbose=False):
    """Configure logging"""
    level = logging.DEBUG if verbose else logging.INFO
    logging.basicConfig(
        level=level,
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    )
    return logging.getLogger(__name__)

def load_config(config_path=None):
    """Load configuration from file or use defaults"""
    config = DEFAULT_CONFIG.copy()
    
    if config_path and Path(config_path).exists():
        import yaml
        with open(config_path, 'r') as f:
            file_config = yaml.safe_load(f)
            config.update(file_config)
    
    return config

def main():
    """Main entry point"""
    args = parse_args()
    logger = setup_logging(args.verbose)
    config = load_config(args.config)
    
    logger.info(f"Starting script (dry_run={args.dry_run})")
    
    try:
        # Your business logic here
        result = run_application(config, args.dry_run)
        logger.info(f"Script completed successfully: {result}")
        return 0
        
    except KeyboardInterrupt:
        logger.warning("Script interrupted by user")
        return 130
    except Exception as e:
        logger.exception(f"Script failed: {e}")
        return 1

def run_application(config, dry_run):
    """Actual application logic"""
    # Implementation
    return {"status": "success"}

if __name__ == "__main__":
    sys.exit(main())
```

---

## Part 2: Configuration Management

### Configuration Hierarchy

```python
import os
import yaml
from pathlib import Path

class Config:
    """
    Configuration with hierarchy:
    1. Default values
    2. Config file
    3. Environment variables
    4. Command-line arguments (passed separately)
    """
    
    DEFAULTS = {
        'server': {
            'host': '0.0.0.0',
            'port': 8080,
            'workers': 4
        },
        'database': {
            'host': 'localhost',
            'port': 5432,
            'name': 'myapp',
            'user': 'appuser'
        },
        'logging': {
            'level': 'INFO',
            'file': '/var/log/myapp.log'
        },
        'retry': {
            'max_attempts': 3,
            'backoff_factor': 2
        }
    }
    
    def __init__(self, config_path=None):
        self._config = self.DEFAULTS.copy()
        
        # Load from file
        if config_path:
            self._load_from_file(config_path)
        
        # Override with environment variables
        self._load_from_env()
    
    def _load_from_file(self, config_path):
        """Load YAML config file"""
        path = Path(config_path)
        if not path.exists():
            raise FileNotFoundError(f"Config file not found: {config_path}")
        
        with open(path, 'r') as f:
            file_config = yaml.safe_load(f)
            self._deep_update(self._config, file_config)
    
    def _load_from_env(self):
        """Override with environment variables"""
        # Example: MYAPP_SERVER_HOST -> config['server']['host']
        prefix = 'MYAPP_'
        for key, value in os.environ.items():
            if key.startswith(prefix):
                parts = key[len(prefix):].lower().split('_')
                self._set_nested(parts, value)
    
    def _set_nested(self, keys, value):
        """Set nested dictionary value from list of keys"""
        config = self._config
        for key in keys[:-1]:
            config = config.setdefault(key, {})
        config[keys[-1]] = self._convert_value(value)
    
    def _convert_value(self, value):
        """Convert string to appropriate type"""
        if value.lower() == 'true':
            return True
        if value.lower() == 'false':
            return False
        if value.isdigit():
            return int(value)
        return value
    
    def _deep_update(self, base, update):
        """Deep update nested dictionaries"""
        for key, value in update.items():
            if isinstance(value, dict) and key in base and isinstance(base[key], dict):
                self._deep_update(base[key], value)
            else:
                base[key] = value
    
    def get(self, path, default=None):
        """Get value by dot-notation path (e.g., 'server.port')"""
        keys = path.split('.')
        config = self._config
        for key in keys:
            if isinstance(config, dict):
                config = config.get(key)
                if config is None:
                    return default
            else:
                return default
        return config
    
    @property
    def all(self):
        return self._config

# Usage
config = Config('/etc/myapp/config.yaml')
print(config.get('server.host'))
print(config.get('database.port'))
```

---

## Part 3: Retry Patterns with Exponential Backoff

### Retry Decorator

```python
import time
import logging
from functools import wraps

logger = logging.getLogger(__name__)

def retry(
    max_attempts=3,
    delay=1,
    backoff=2,
    exceptions=(Exception,)
):
    """
    Retry decorator with exponential backoff
    
    Args:
        max_attempts: Maximum number of attempts
        delay: Initial delay in seconds
        backoff: Multiplier for delay (exponential backoff)
        exceptions: Tuple of exceptions to retry on
    """
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            last_exception = None
            current_delay = delay
            
            for attempt in range(1, max_attempts + 1):
                try:
                    return func(*args, **kwargs)
                except exceptions as e:
                    last_exception = e
                    if attempt == max_attempts:
                        logger.error(f"Failed after {max_attempts} attempts")
                        raise
                    
                    logger.warning(
                        f"Attempt {attempt}/{max_attempts} failed: {e}. "
                        f"Retrying in {current_delay}s..."
                    )
                    time.sleep(current_delay)
                    current_delay *= backoff
            
            raise last_exception
        return wrapper
    return decorator

# Usage
@retry(max_attempts=5, delay=1, backoff=2, exceptions=(requests.RequestException,))
def fetch_data(url):
    response = requests.get(url, timeout=5)
    response.raise_for_status()
    return response.json()
```

### Retry with Jitter (Avoid Thundering Herd)

```python
import random

def retry_with_jitter(max_attempts=3, base_delay=1, max_delay=30):
    """Retry with random jitter to avoid synchronized retries"""
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            last_exception = None
            
            for attempt in range(1, max_attempts + 1):
                try:
                    return func(*args, **kwargs)
                except Exception as e:
                    last_exception = e
                    if attempt == max_attempts:
                        raise
                    
                    # Calculate delay with jitter
                    delay = min(base_delay * (2 ** (attempt - 1)), max_delay)
                    jitter = random.uniform(0, delay * 0.1)
                    total_delay = delay + jitter
                    
                    logger.warning(f"Attempt {attempt} failed. Retrying in {total_delay:.2f}s")
                    time.sleep(total_delay)
            
            raise last_exception
        return wrapper
    return decorator
```

---

## Part 4: Graceful Shutdown (Signal Handling)

```python
import signal
import sys
import time
import logging

logger = logging.getLogger(__name__)

class GracefulShutdown:
    """Handle SIGTERM and SIGINT for graceful shutdown"""
    
    def __init__(self):
        self.shutdown_requested = False
        self.original_handlers = {}
    
    def setup(self):
        """Register signal handlers"""
        self.original_handlers[signal.SIGTERM] = signal.signal(
            signal.SIGTERM, self._handle_shutdown
        )
        self.original_handlers[signal.SIGINT] = signal.signal(
            signal.SIGINT, self._handle_shutdown
        )
    
    def restore(self):
        """Restore original signal handlers"""
        for sig, handler in self.original_handlers.items():
            signal.signal(sig, handler)
    
    def _handle_shutdown(self, signum, frame):
        """Handle shutdown signal"""
        logger.info(f"Received signal {signum}, initiating graceful shutdown...")
        self.shutdown_requested = True
    
    def should_shutdown(self):
        return self.shutdown_requested
    
    def wait_for_shutdown(self, timeout=30):
        """Wait for shutdown with timeout"""
        start = time.time()
        while not self.shutdown_requested and (time.time() - start) < timeout:
            time.sleep(0.1)
        return self.shutdown_requested

# Usage in main loop
def main_loop():
    graceful = GracefulShutdown()
    graceful.setup()
    
    logger.info("Starting main loop")
    
    try:
        while not graceful.should_shutdown():
            # Do work
            time.sleep(1)
            logger.debug("Working...")
    finally:
        logger.info("Cleaning up resources...")
        # Close connections, flush logs, etc.
        graceful.restore()
        logger.info("Shutdown complete")
```

### Context Manager for Graceful Shutdown

```python
import signal
from contextlib import contextmanager

@contextmanager
def graceful_shutdown():
    """Context manager for graceful shutdown"""
    shutdown_requested = False
    
    def handler(signum, frame):
        nonlocal shutdown_requested
        shutdown_requested = True
        print(f"\nReceived signal {signum}, shutting down...")
    
    original_handlers = {
        signal.SIGTERM: signal.signal(signal.SIGTERM, handler),
        signal.SIGINT: signal.signal(signal.SIGINT, handler),
    }
    
    try:
        yield lambda: shutdown_requested
    finally:
        for sig, handler in original_handlers.items():
            signal.signal(sig, handler)

# Usage
with graceful_shutdown() as should_shutdown:
    while not should_shutdown():
        # Do work
        pass
```

---

## Part 5: Timeout Patterns

### Function Timeout with `signal`

```python
import signal
from contextlib import contextmanager

class TimeoutError(Exception):
    pass

@contextmanager
def timeout(seconds):
    """Context manager for function timeout (Unix only)"""
    def handler(signum, frame):
        raise TimeoutError(f"Operation timed out after {seconds} seconds")
    
    original_handler = signal.signal(signal.SIGALRM, handler)
    signal.alarm(seconds)
    
    try:
        yield
    finally:
        signal.alarm(0)
        signal.signal(signal.SIGALRM, original_handler)

# Usage
try:
    with timeout(5):
        time.sleep(10)  # Will timeout
except TimeoutError as e:
    print(e)
```

### Timeout with `threading`

```python
import threading
import time
from functools import wraps

class TimeoutError(Exception):
    pass

def timeout(seconds):
    """Decorator for function timeout using threads"""
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            result = [None]
            error = [None]
            
            def target():
                try:
                    result[0] = func(*args, **kwargs)
                except Exception as e:
                    error[0] = e
            
            thread = threading.Thread(target=target)
            thread.daemon = True
            thread.start()
            thread.join(seconds)
            
            if thread.is_alive():
                raise TimeoutError(f"Function {func.__name__} timed out after {seconds}s")
            
            if error[0]:
                raise error[0]
            
            return result[0]
        return wrapper
    return decorator

# Usage
@timeout(5)
def slow_function():
    time.sleep(10)
    return "Done"

# This will raise TimeoutError
```

---

## Part 6: Rate Limiting

```python
import time
import threading
from functools import wraps

class RateLimiter:
    """Rate limiter for API calls"""
    
    def __init__(self, calls_per_second=1):
        self.calls_per_second = calls_per_second
        self.min_interval = 1.0 / calls_per_second
        self.last_call_time = 0
        self.lock = threading.Lock()
    
    def wait(self):
        """Wait if needed to respect rate limit"""
        with self.lock:
            now = time.time()
            time_since_last = now - self.last_call_time
            if time_since_last < self.min_interval:
                sleep_time = self.min_interval - time_since_last
                time.sleep(sleep_time)
            self.last_call_time = time.time()
    
    def __call__(self, func):
        """Decorator version"""
        @wraps(func)
        def wrapper(*args, **kwargs):
            self.wait()
            return func(*args, **kwargs)
        return wrapper

# Usage
rate_limiter = RateLimiter(calls_per_second=10)

@rate_limiter
def api_call():
    # This will be rate-limited to 10 calls per second
    pass
```

### Token Bucket Rate Limiter

```python
import time
import threading

class TokenBucket:
    """Token bucket rate limiter with burst capacity"""
    
    def __init__(self, rate=1, capacity=1):
        self.rate = rate  # Tokens per second
        self.capacity = capacity
        self.tokens = capacity
        self.last_refill = time.time()
        self.lock = threading.Lock()
    
    def _refill(self):
        now = time.time()
        elapsed = now - self.last_refill
        new_tokens = elapsed * self.rate
        self.tokens = min(self.capacity, self.tokens + new_tokens)
        self.last_refill = now
    
    def acquire(self, tokens=1, timeout=None):
        """Acquire tokens, returns True if successful"""
        start = time.time()
        
        while True:
            with self.lock:
                self._refill()
                if self.tokens >= tokens:
                    self.tokens -= tokens
                    return True
            
            if timeout is not None and (time.time() - start) > timeout:
                return False
            
            time.sleep(0.01)  # Small sleep before retry

# Usage
bucket = TokenBucket(rate=5, capacity=10)  # 5 per second, burst up to 10

for i in range(20):
    if bucket.acquire():
        print(f"Request {i} allowed")
    else:
        print(f"Request {i} rate limited")
```

---

## Part 7: Complete Production Script Example

```python
#!/usr/bin/env python3
"""
Production-ready deployment script
"""

import argparse
import logging
import os
import signal
import sys
import time
from pathlib import Path
from typing import Dict, Any

import yaml
import requests

# ============================================================================
# Configuration
# ============================================================================

class Config:
    """Configuration management"""
    
    DEFAULTS = {
        'api': {
            'base_url': 'https://api.example.com',
            'timeout': 30,
            'retry_attempts': 3,
            'retry_backoff': 2
        },
        'deployment': {
            'namespace': 'default',
            'wait_timeout': 300,
            'health_check_interval': 5
        },
        'logging': {
            'level': 'INFO',
            'file': '/var/log/deploy.log'
        }
    }
    
    def __init__(self, config_path=None):
        self._config = self.DEFAULTS.copy()
        if config_path:
            self._load_file(config_path)
        self._load_env()
    
    def _load_file(self, path):
        with open(path, 'r') as f:
            data = yaml.safe_load(f)
            self._deep_update(self._config, data)
    
    def _load_env(self):
        prefix = 'DEPLOY_'
        for key, value in os.environ.items():
            if key.startswith(prefix):
                parts = key[len(prefix):].lower().split('__')
                self._set_nested(parts, value)
    
    def _set_nested(self, keys, value):
        config = self._config
        for key in keys[:-1]:
            config = config.setdefault(key, {})
        config[keys[-1]] = value
    
    def _deep_update(self, base, update):
        for key, value in update.items():
            if isinstance(value, dict) and key in base:
                self._deep_update(base[key], value)
            else:
                base[key] = value
    
    def get(self, path, default=None):
        keys = path.split('.')
        value = self._config
        for key in keys:
            if isinstance(value, dict):
                value = value.get(key)
                if value is None:
                    return default
            else:
                return default
        return value

# ============================================================================
# Logging
# ============================================================================

def setup_logging(config: Config) -> logging.Logger:
    """Configure logging based on config"""
    log_level = getattr(logging, config.get('logging.level', 'INFO'))
    log_file = config.get('logging.file')
    
    handlers = [logging.StreamHandler()]
    if log_file:
        Path(log_file).parent.mkdir(parents=True, exist_ok=True)
        handlers.append(logging.FileHandler(log_file))
    
    logging.basicConfig(
        level=log_level,
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
        handlers=handlers
    )
    
    return logging.getLogger(__name__)

# ============================================================================
# API Client with Retry
# ============================================================================

class APIClient:
    def __init__(self, config: Config, logger: logging.Logger):
        self.config = config
        self.logger = logger
        self.base_url = config.get('api.base_url')
        self.timeout = config.get('api.timeout')
    
    def _request_with_retry(self, method, endpoint, **kwargs):
        """Make request with retry logic"""
        url = f"{self.base_url}/{endpoint.lstrip('/')}"
        max_attempts = self.config.get('api.retry_attempts', 3)
        backoff = self.config.get('api.retry_backoff', 2)
        
        for attempt in range(1, max_attempts + 1):
            try:
                response = requests.request(
                    method, url, timeout=self.timeout, **kwargs
                )
                response.raise_for_status()
                return response.json()
            except requests.exceptions.RequestException as e:
                if attempt == max_attempts:
                    self.logger.error(f"Request failed after {max_attempts} attempts")
                    raise
                
                wait = backoff ** (attempt - 1)
                self.logger.warning(f"Attempt {attempt} failed: {e}. Retrying in {wait}s")
                time.sleep(wait)
    
    def get(self, endpoint, **kwargs):
        return self._request_with_retry('GET', endpoint, **kwargs)
    
    def post(self, endpoint, **kwargs):
        return self._request_with_retry('POST', endpoint, **kwargs)

# ============================================================================
# Graceful Shutdown
# ============================================================================

class GracefulShutdown:
    def __init__(self, logger: logging.Logger):
        self.logger = logger
        self.shutdown_requested = False
    
    def setup(self):
        signal.signal(signal.SIGTERM, self._handler)
        signal.signal(signal.SIGINT, self._handler)
    
    def _handler(self, signum, frame):
        self.logger.info(f"Received signal {signum}, shutting down...")
        self.shutdown_requested = True
    
    def should_shutdown(self):
        return self.shutdown_requested

# ============================================================================
# Main Application
# ============================================================================

class Deployer:
    def __init__(self, config: Config, logger: logging.Logger, dry_run=False):
        self.config = config
        self.logger = logger
        self.dry_run = dry_run
        self.api = APIClient(config, logger)
    
    def run(self):
        """Main deployment logic"""
        self.logger.info(f"Starting deployment (dry_run={self.dry_run})")
        
        try:
            # Step 1: Validate
            self.logger.info("Step 1: Validating configuration")
            self._validate()
            
            # Step 2: Build
            self.logger.info("Step 2: Building artifacts")
            self._build()
            
            # Step 3: Deploy
            self.logger.info("Step 3: Deploying")
            self._deploy()
            
            # Step 4: Verify
            self.logger.info("Step 4: Verifying deployment")
            self._verify()
            
            self.logger.info("Deployment completed successfully")
            return 0
            
        except Exception as e:
            self.logger.exception(f"Deployment failed: {e}")
            return 1
    
    def _validate(self):
        if self.dry_run:
            self.logger.info("[DRY RUN] Would validate configuration")
            return
        # Actual validation logic
    
    def _build(self):
        if self.dry_run:
            self.logger.info("[DRY RUN] Would build artifacts")
            return
        # Actual build logic
    
    def _deploy(self):
        if self.dry_run:
            self.logger.info("[DRY RUN] Would deploy to cluster")
            return
        # Actual deployment logic
    
    def _verify(self):
        if self.dry_run:
            self.logger.info("[DRY RUN] Would verify deployment")
            return
        # Actual verification logic

# ============================================================================
# Main Entry Point
# ============================================================================

def parse_args():
    parser = argparse.ArgumentParser(description='Production Deployment Script')
    parser.add_argument('-c', '--config', help='Configuration file path')
    parser.add_argument('-v', '--verbose', action='store_true', help='Verbose output')
    parser.add_argument('-n', '--dry-run', action='store_true', help='Dry run (no changes)')
    return parser.parse_args()

def main():
    args = parse_args()
    
    # Load configuration
    config = Config(args.config)
    
    # Setup logging
    if args.verbose:
        os.environ['DEPLOY_LOGGING__LEVEL'] = 'DEBUG'
    
    logger = setup_logging(config)
    logger.info("Deployment script started")
    
    # Setup graceful shutdown
    graceful = GracefulShutdown(logger)
    graceful.setup()
    
    # Run deployer
    deployer = Deployer(config, logger, args.dry_run)
    exit_code = deployer.run()
    
    logger.info(f"Deployment script finished with exit code {exit_code}")
    return exit_code

if __name__ == "__main__":
    sys.exit(main())
```

---

## Quick Task: Build a Production-Ready Script

*Take a simple script and make it production-ready.*

1. Add argument parsing with `--verbose` and `--dry-run`.
2. Add logging (console + file).
3. Add retry logic for network calls.
4. Add graceful shutdown handling.

> **Ready Solution:**
>
> ```python
> # production_ready.py
> import argparse
> import logging
> import signal
> import sys
> import time
> from functools import wraps
>
> # Retry decorator
> def retry(max_attempts=3, delay=1):
>     def decorator(func):
>         @wraps(func)
>         def wrapper(*args, **kwargs):
>             for attempt in range(1, max_attempts + 1):
>                 try:
>                     return func(*args, **kwargs)
>                 except Exception as e:
>                     if attempt == max_attempts:
>                         raise
>                     logging.warning(f"Attempt {attempt} failed: {e}. Retrying...")
>                     time.sleep(delay)
>             return None
>         return wrapper
>     return decorator
>
> @retry(max_attempts=3)
> def call_api():
>     # Simulate API call
>     import requests
>     response = requests.get('https://api.example.com/health', timeout=5)
>     response.raise_for_status()
>     return response.json()
>
> def setup_logging(verbose):
>     level = logging.DEBUG if verbose else logging.INFO
>     logging.basicConfig(
>         level=level,
>         format='%(asctime)s - %(levelname)s - %(message)s',
>         handlers=[
>             logging.StreamHandler(),
>             logging.FileHandler('app.log')
>         ]
>     )
>
> def main():
>     parser = argparse.ArgumentParser()
>     parser.add_argument('-v', '--verbose', action='store_true')
>     parser.add_argument('-n', '--dry-run', action='store_true')
>     args = parser.parse_args()
>     
>     setup_logging(args.verbose)
>     logging.info(f"Starting (dry_run={args.dry_run})")
>     
>     if args.dry_run:
>         logging.info("DRY RUN: Would call API")
>         return 0
>     
>     try:
>         result = call_api()
>         logging.info(f"API response: {result}")
>         return 0
>     except Exception as e:
>         logging.error(f"Failed: {e}")
>         return 1
>
> if __name__ == "__main__":
>     sys.exit(main())
> ```

---

## Summary Table: Production Patterns

### Script Structure

| Component | Purpose |
|-----------|---------|
| Shebang | `#!/usr/bin/env python3` |
| `if __name__ == "__main__"` | Allow imports without execution |
| `parse_args()` | CLI argument parsing |
| `setup_logging()` | Logging configuration |
| `main()` | Returns exit code |
| `sys.exit(main())` | Proper exit |

### Configuration Sources

| Source | Priority |
|--------|----------|
| Defaults | Lowest |
| Config file | Medium |
| Environment variables | High |
| Command line | Highest |

### Retry Patterns

| Pattern | Use Case |
|---------|----------|
| Fixed delay | Simple retries |
| Exponential backoff | Network calls |
| With jitter | Avoid thundering herd |
| Circuit breaker | Prevent cascading failures |

### Signal Handling

| Signal | When Sent | Default Action |
|--------|-----------|----------------|
| SIGTERM (15) | `kill` command | Terminate |
| SIGINT (2) | Ctrl+C | Interrupt |
| SIGHUP (1) | Terminal closed | Hangup |

---

**Next note (9.4.3)** will be the Subchapter Review for Testing and Production-Ready Patterns, plus the **Final Exam for Module 9** covering all Python topics.

**Backward references:**
- Logging from 9.3.1 (production logging)
- Exception handling from 9.3.1 (retry patterns)
- Environment variables from 9.2.2 (config management)
- Testing from 9.4.1 (testing production code)
